plugins {
    id 'maven-publish'
}

version '1.0.0'

publishing {
    publications {
        maven(MavenPublication) {
            artifactId = 'void-framework-bukkit-api-legacy'
            from components.java
        }
    }
    repositories {
        mavenLocal()
    }
}