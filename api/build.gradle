plugins {
    id 'maven-publish'
}

version '2.0.0'

publishing {
    publications {
        maven(MavenPublication) {
            artifactId = 'void-framework-bukkit-api'
            from components.java
        }
    }
    repositories {
        mavenLocal()
    }
}